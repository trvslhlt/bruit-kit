{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    // See the matching comment in demo/tsconfig.json -- e2e specs import
    // from ../demo and ../src, so rootDir needs to cover the whole repo,
    // not just e2e/ (and not the parent config's inherited "src").
    "rootDir": ".."
  },
  "include": ["."]
}
